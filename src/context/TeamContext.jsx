import {createContext,useContext,useEffect,useState}from'react';import{supabase}from'../lib/supabase';import{useAuth}from'./AuthContext';const C=createContext(null);export function TeamProvider({children}){const{user}=useAuth(),[teams,setTeams]=useState([]),[team,setTeam]=useState(null),[courses,setCourses]=useState([]),[videos,setVideos]=useState({}),[progress,setProgress]=useState(new Set()),[loading,setLoading]=useState(false);async function load(){if(!user)return;setLoading(true);const{data:ms}=await supabase.from('team_members').select('team_id,role,joined_at,teams(id,name,code,created_by,created_at)').eq('user_id',user.id);const ts=(ms||[]).map(x=>({...x.teams,role:x.role}));setTeams(ts);const saved=localStorage.getItem('lb-team');const t=ts.find(x=>x.id===saved)||ts[0]||null;setTeam(t);if(t){const{data:p}=await supabase.from('playlists').select('*').eq('team_id',t.id).order('created_at',{ascending:true});setCourses(p||[]);const ids=(p||[]).map(x=>x.id);if(ids.length){const{data:v}=await supabase.from('playlist_videos').select('*').in('playlist_id',ids).order('position');const map={};(v||[]).forEach(x=>(map[x.playlist_id]??=[]).push(x));setVideos(map);const vids=(v||[]).map(x=>x.id);if(vids.length){const{data:pr}=await supabase.from('video_progress').select('playlist_video_id,completed_at').eq('user_id',user.id).in('playlist_video_id',vids);setProgress(new Set((pr||[]).filter(x=>x.completed_at).map(x=>x.playlist_video_id)))}}}setLoading(false)}useEffect(()=>{load()},[user]);function switchTeam(id){const t=teams.find(x=>x.id===id);if(!t)return;localStorage.setItem('lb-team',id);setTeam(t);load()}return <C.Provider value={{teams,team,courses,videos,progress,loading,reload:load,switchTeam}}>{children}</C.Provider>}export const useTeam=()=>useContext(C);