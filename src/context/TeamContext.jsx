import{createContext,useContext,useEffect,useState}from'react';import{supabase}from'../lib/supabase';import{useAuth}from'./AuthContext';const C=createContext(null);export function TeamProvider({children}){const{user}=useAuth(),[teams,setTeams]=useState([]),[team,setTeam]=useState(null),[courses,setCourses]=useState([]),[videos,setVideos]=useState({}),[progress,setProgress]=useState({}),[chapters,setChapters]=useState({}),[loading,setLoading]=useState(false);async function load(){if(!user)return;setLoading(true);const{data:ms}=await supabase.from('team_members').select('team_id,role,joined_at,teams(id,name,code,created_by,created_at)').eq('user_id',user.id);const ts=(ms||[]).map(x=>({...x.teams,role:x.role}));setTeams(ts);const saved=localStorage.getItem('lb-team'),t=ts.find(x=>x.id===saved)||ts[0]||null;setTeam(t);if(t){const{data:p}=await supabase.from('playlists').select('*').eq('team_id',t.id).order('created_at',{ascending:true});setCourses(p||[]);const ids=(p||[]).map(x=>x.id);if(ids.length){const{data:v}=await supabase.from('playlist_videos').select('*').in('playlist_id',ids).order('position');const map={};(v||[]).forEach(x=>(map[x.playlist_id]??=[]).push(x));setVideos(map);const vids=(v||[]).map(x=>x.id);if(vids.length){const[{data:pr},{data:ch}]=await Promise.all([supabase.from('video_progress').select('*').eq('user_id',user.id).in('playlist_video_id',vids),supabase.from('video_chapters').select('*').in('playlist_video_id',vids).order('position')]);const pm={};(pr||[]).forEach(x=>pm[x.playlist_video_id]=x);setProgress(pm);const cm={};(ch||[]).forEach(x=>(cm[x.playlist_video_id]??=[]).push(x));setChapters(cm)}}}setLoading(false)}useEffect(()=>{load()},[user]);function switchTeam(id){const t=teams.find(x=>x.id===id);if(!t)return;localStorage.setItem('lb-team',id);setTeam(t);load()}useEffect(()=>{if(!user)return;const sub=supabase.channel('learning-react').on('postgres_changes',{event:'*',schema:'public',table:'video_progress',filter:'user_id=eq.'+user.id},()=>load()).on('postgres_changes',{event:'*',schema:'public',table:'video_notes'},()=>{}).subscribe();return()=>supabase.removeChannel(sub)},[user,team?.id]);return <C.Provider value={{teams,team,courses,videos,progress,chapters,loading,reload:load,switchTeam}}>{children}</C.Provider>}export const useTeam=()=>useContext(C);