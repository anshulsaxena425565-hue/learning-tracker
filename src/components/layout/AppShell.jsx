import{useState}from'react';import{useAuth}from'../../context/AuthContext';import{useTeam}from'../../context/TeamContext';import{supabase}from'../../lib/supabase';const nav=[['dashboard','Dashboard'],['courses','Courses'],['teams','Teams'],['leaderboard','Leaderboard'],['profile','Profile']];export default function AppShell({children,view,setView}){const{user}=useAuth(),{team}=useTeam();return <div className="app"><aside><div className="brand">Learning<span>Beyond</span></div><div className="team-chip">{team?.name||'Your team'}</div><nav className="nav">{nav.map(([id,label])=><button className={view===id?'active':''} onClick={()=>setView(id)} key={id}>{label}</button>)}</nav><button className="signout" onClick={()=>supabase.auth.signOut()}>Sign out</button></aside><main className="content">{children}</main></div>}