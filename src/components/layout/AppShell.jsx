import{useState}from'react';import{useAuth}from'../../context/AuthContext';import{useTeam}from'../../context/TeamContext';import{supabase}from'../../lib/supabase';const base=[['dashboard','⌂','Dashboard'],['courses','▣','Courses'],['teams','◉','Teams'],['leaderboard','↗','Leaderboard'],['profile','◎','Profile']];export default function AppShell({children,view,setView}){const{user}=useAuth(),{team,teams,switchTeam}=useTeam(),[open,setOpen]=useState(false);const isAdmin=['owner','admin'].includes(team?.role);return <div className="app"><aside className="sidebar"><div className="brand">Learning<span>Beyond</span></div><div className="workspace"><span>{team?.name||'Your team'}</span><select value={team?.id||''} onChange={e=>switchTeam(e.target.value)}>{teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div><nav className="nav">{base.map(([id,icon,label])=><button className={view===id?'active':''} onClick={()=>{setView(id);setOpen(false)}} key={id}><span>{icon}</span>{label}</button>)}{isAdmin&&<button className={view==='add'?'active':''} onClick={()=>{setView('add');setOpen(false)}}><span>＋</span>Add course</button>}</nav><div className="sidebar-bottom"><button className="profile-mini" onClick={()=>setView('profile')}><span className="avatar-dot">{(user?.email||'L')[0].toUpperCase()}</span><span>{user?.email}</span></button><button className="signout" onClick={()=>supabase.auth.signOut()}>Sign out</button></div></aside><header className="mobile-head"><div className="brand">Learning<span>Beyond</span></div><button onClick={()=>setOpen(!open)}>☰</button></header>{open&&<div className="mobile-nav">{base.map(([id,icon,label])=><button onClick={()=>{setView(id);setOpen(false)}} key={id}>{icon} {label}</button>)}{isAdmin&&<button onClick={()=>{setView('add');setOpen(false)}}>＋ Add course</button>}</div>}<main className="content">{children}</main></div>}