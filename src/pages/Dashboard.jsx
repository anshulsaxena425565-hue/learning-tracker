import{useMemo}from'react';import{useTeam}from'../context/TeamContext';
export default function Dashboard({openCourse}){const{courses,videos,progress,team}=useTeam();const stats=useMemo(()=>{const all=Object.values(videos).flat(),done=all.filter(v=>progress[v.id]?.completed_at).length,p=Math.round(done/(all.length||1)*100),next=all.find(v=>!progress[v.id]?.completed_at);return{all,done,p,next}},[videos,progress]);const active=stats.all.filter(v=>progress[v.id]?.watched_seconds>0&&!progress[v.id]?.completed_at).length;return <div className="page">
<header><div><p className="eyebrow">LEARNING PULSE</p><h1>Good to see you.</h1><p className="muted">{team?.name} · Keep building momentum.</p></div><div className="pill">✦ {active} in progress</div></header>
<section className="kpi-grid"><div className="kpi-card"><span className="kpi-label">Learning Pulse</span><strong>{stats.p}%</strong><small>overall completion</small><div className="progress"><i style={{width:stats.p+'%'}}/></div></div><div className="kpi-card"><span className="kpi-label">Completion Rate</span><strong>{stats.done}</strong><small>lessons completed</small></div><div className="kpi-card"><span className="kpi-label">Next Milestone</span><strong>{stats.next?'Keep going':'All done'}</strong><small>{stats.next?'next lesson waiting':'team library complete'}</small></div><div className="kpi-card"><span className="kpi-label">Team Activity</span><strong>{courses.length}</strong><small>courses available</small></div></section>
<section className="hero dashboard-hero"><div><p className="eyebrow">YOUR PROGRESS</p><h2>{stats.p}% course completion</h2><p className="muted">{stats.done} of {stats.all.length} lessons completed across your team courses.</p>{stats.next&&<button onClick={()=>{const c=courses.find(c=>(videos[c.id]||[]).some(v=>v.id===stats.next.id));if(c)openCourse(c.id)}}>Continue learning →</button>}</div><div className="hero-ring" style={{'--pct':stats.p+'%'}}><b>{stats.p}%</b></div></section>
<section className="section"><div className="section-head"><div><p className="eyebrow">YOUR COURSES</p><h2>Continue learning</h2></div><span className="muted">{courses.length} courses</span></div><div className="course-grid">{courses.map(c=>{const vs=videos[c.id]||[],d=vs.filter(v=>progress[v.id]?.completed_at).length,p=Math.round(d/(vs.length||1)*100),next=vs.find(v=>!progress[v.id]?.completed_at);return <article className="course-card item click" key={c.id} onClick={()=>openCourse(c.id)}><div className="course-ring" style={{'--pct':p+'%'}}><b>{p}%</b></div><div className="grow"><h3>{c.title}</h3><p className="course-meta">{c.channel||'Course'} · {d}/{vs.length} completed</p><div className="progress mini"><i style={{width:p+'%'}}/></div></div><span className="arrow">→</span></article>})}</div>{!courses.length&&<div className="empty">No courses found for this team.</div>}</section></div>}