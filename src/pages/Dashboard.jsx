import{useMemo}from'react';import{useTeam}from'../context/TeamContext';
export default function Dashboard({openCourse}){const{courses,videos,progress,team}=useTeam();const stats=useMemo(()=>{const all=Object.values(videos).flat(),done=all.filter(v=>progress[v.id]?.completed_at).length,p=Math.round(done/(all.length||1)*100),activeList=all.filter(v=>progress[v.id]?.watched_seconds>0&&!progress[v.id]?.completed_at).sort((a,b)=>new Date(progress[b.id]?.updated_at||0)-new Date(progress[a.id]?.updated_at||0)),next=activeList[0]||all.find(v=>!progress[v.id]?.completed_at);const active=activeList.length;return{all,done,p,next,active}},[videos,progress]);const milestones=[{label:'First step',target:1,copy:'Complete your first lesson',icon:'✦'},{label:'Quarter way',target:Math.max(1,Math.ceil(stats.all.length*.25)),copy:'Reach 25% overall progress',icon:'◒'},{label:'Halfway',target:Math.max(1,Math.ceil(stats.all.length*.5)),copy:'Reach 50% overall progress',icon:'◉'},{label:'Finish line',target:Math.max(1,stats.all.length),copy:'Complete the whole library',icon:'★'}];function continueCourse(){if(!stats.next)return;const c=courses.find(c=>(videos[c.id]||[]).some(v=>v.id===stats.next.id));if(c)openCourse(c.id)}return <div className="page dashboard-page">
<header className="dashboard-header"><div><p className="eyebrow">LEARNING PULSE</p><h1>Good to see you.</h1><p className="muted">{team?.name||'Your workspace'} <span className="dot-sep">·</span> Keep building momentum.</p></div><span className="momentum-chip"><span className="live-dot"/>{stats.active} {stats.active===1?'lesson':'lessons'} in progress</span></header>
<section className="dashboard-hero modern-hero"><div className="hero-pattern"/><div className="modern-hero-copy"><div className="hero-kicker"><span>YOUR MOMENTUM</span><b>{stats.p}%</b></div><h2>{stats.p===0?'Start your learning journey.':stats.p<50?'You’re building momentum.':'You’re on a roll.'}</h2><p>{stats.done} of {stats.all.length} lessons completed across your team library.</p><div className="hero-actions"><button onClick={continueCourse} disabled={!stats.next}>{stats.next?'Continue learning':'Library complete'} <span>→</span></button><span className="hero-caption">Every lesson counts.</span></div></div><div className="hero-progress"><div className="hero-ring" style={{'--pct':stats.p+'%'}}><b>{stats.p}%</b></div><span className="hero-progress-label">OVERALL<br/>PROGRESS</span></div></section>
<section className="kpi-grid modern-kpis"><div className="kpi-card modern-kpi"><span className="kpi-icon">↗</span><span className="kpi-label">Learning pulse</span><strong>{stats.p}%</strong><small>overall completion</small></div><div className="kpi-card modern-kpi"><span className="kpi-icon">✓</span><span className="kpi-label">Completed</span><strong>{stats.done}</strong><small>lessons completed</small></div><div className="kpi-card modern-kpi"><span className="kpi-icon">◎</span><span className="kpi-label">Next up</span><strong>{stats.next?'Keep going':'All done'}</strong><small>{stats.next?'one lesson waiting':'library complete'}</small></div><div className="kpi-card modern-kpi"><span className="kpi-icon">▦</span><span className="kpi-label">Your library</span><strong>{courses.length}</strong><small>courses available</small></div></section>
<section className="milestones-section"><div className="section-head"><div><p className="eyebrow">MILESTONES</p><h2>Build your streak of wins</h2><p className="muted">Small checkpoints that turn progress into momentum.</p></div><span className="milestone-summary">{stats.done}/{stats.all.length} lessons</span></div><div className="milestone-grid">{milestones.map((m,i)=>{const value=Math.min(stats.done,m.target),pct=Math.round(value/m.target*100),reached=stats.done>=m.target;return <article className={'milestone-card '+(reached?'reached':'')} key={m.label} style={{'--delay':i*70+'ms'}}><div className="milestone-top"><div className="milestone-ring" style={{'--pct':pct+'%'}}><span>{reached?'✓':m.icon}</span></div><span className={reached?'status-pill done':'status-pill'}>{reached?'Unlocked':pct+'%'}</span></div><div><h3>{m.label}</h3><p>{m.copy}</p></div><div className="milestone-track"><i style={{width:pct+'%'}}/></div><small>{value} / {m.target} lessons</small></article>})}</div></section>
<section className="section dashboard-courses"><div className="section-head"><div><p className="eyebrow">YOUR COURSES</p><h2>Continue learning</h2></div><span className="muted">{courses.length} courses</span></div><div className="course-grid">{courses.map((c,i)=>{const vs=videos[c.id]||[],d=vs.filter(v=>progress[v.id]?.completed_at).length,p=Math.round(d/(vs.length||1)*100);return <article className="course-card item click modern-course-card" key={c.id} style={{'--delay':i*60+'ms'}} onClick={()=>openCourse(c.id)}><div className="course-ring" style={{'--pct':p+'%'}}><b>{p}%</b></div><div className="grow"><span className="course-index">COURSE {String(i+1).padStart(2,'0')}</span><h3>{c.title}</h3><p className="course-meta">{c.channel||'Course'} · {d}/{vs.length} completed</p><div className="progress mini"><i style={{width:p+'%'}}/></div></div><span className="arrow">↗</span></article>})}</div>{!courses.length&&<div className="empty">No courses found for this team.</div>}</section></div>}