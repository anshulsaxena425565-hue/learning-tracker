import{useEffect,useRef,useState}from'react';import{useTeam}from'../context/TeamContext';import{useAuth}from'../context/AuthContext';import{supabase}from'../lib/supabase';
function time(s){s=Math.max(0,Math.floor(s||0));return String(Math.floor(s/3600)).padStart(2,'0')+':'+String(Math.floor(s%3600/60)).padStart(2,'0')+':'+String(s%60).padStart(2,'0')}
export default function CoursePlayer({courseId,videoId,back}){const{user}=useAuth(),{courses,videos,progress,chapters}=useTeam(),[tab,setTab]=useState('outline'),[notes,setNotes]=useState([]),[transcript,setTranscript]=useState(null),[bookmarks,setBookmarks]=useState([]),[note,setNote]=useState(''),[playing,setPlaying]=useState(false),[watched,setWatched]=useState(progress[videoId]?.watched_seconds||0),player=useRef(null),timer=useRef(null),last=useRef(null);const p=courses.find(x=>x.id===courseId),vs=videos[courseId]||[],v=vs.find(x=>x.id===videoId)||vs[0],ch=chapters[v?.id]||[];useEffect(()=>{if(!v)return;let mounted=true;const load=()=>{if(!mounted)return;player.current=new window.YT.Player('yt-player',{videoId:v.youtube_video_id||undefined,width:'100%',height:'100%',playerVars:{rel:0,modestbranding:1},events:{onReady:e=>{if(progress[v.id]?.last_position_seconds)e.target.seekTo(progress[v.id].last_position_seconds,true)},onStateChange:e=>{const Y=window.YT;if(e.data===Y.PlayerState.PLAYING){setPlaying(true);last.current=e.target.getCurrentTime();clearInterval(timer.current);timer.current=setInterval(()=>{const now=e.target.getCurrentTime(),delta=now-(last.current??now);if(delta>=0&&delta<=3.5){setWatched(x=>x+delta);save(e.target.getCurrentTime(),e.target.getDuration())}last.current=now},2000)}else{setPlaying(false);clearInterval(timer.current);if(e.data===Y.PlayerState.ENDED)finish(e.target.getDuration())}}}})};if(window.YT?.Player)load();else{window.onYouTubeIframeAPIReady=load;const s=document.createElement('script');s.src='https://www.youtube.com/iframe_api';s.async=true;document.body.appendChild(s)}return()=>{mounted=false;clearInterval(timer.current);try{player.current?.destroy()}catch{}}},[v?.id]);useEffect(()=>{if(tab==='notes')loadNotes();if(tab==='transcript')loadTranscript();if(tab==='bookmarks')loadBookmarks()},[tab,v?.id]);async function save(pos,dur){if(!user||!v)return;await supabase.from('video_progress').upsert({user_id:user.id,playlist_video_id:v.id,watched_seconds:Math.floor(watched),duration_seconds:Math.floor(dur||0),last_position_seconds:Math.floor(pos)},{onConflict:'user_id,playlist_video_id'})}async function finish(dur){await supabase.from('video_progress').upsert({user_id:user.id,playlist_video_id:v.id,watched_seconds:Math.floor(Math.max(watched,dur)),duration_seconds:Math.floor(dur||0),last_position_seconds:0,completed_at:new Date().toISOString()},{onConflict:'user_id,playlist_video_id'});setWatched(dur)}async function loadNotes(){const{data}=await supabase.from('video_notes').select('id,content,user_id,created_at,position_seconds').eq('playlist_video_id',v.id).order('created_at',{ascending:false});setNotes(data||[])}async function saveNote(){if(!note.trim())return;await supabase.from('video_notes').insert({playlist_video_id:v.id,user_id:user.id,content:note.trim(),position_seconds:Math.floor(player.current?.getCurrentTime?.()||0)});setNote('');loadNotes()}async function loadBookmarks(){const{data}=await supabase.from('video_bookmarks').select('*').eq('playlist_video_id',v.id).eq('user_id',user.id).order('position_seconds');setBookmarks(data||[])}async function bookmark(){const pos=Math.floor(player.current?.getCurrentTime?.()||0);if(!pos)return;await supabase.from('video_bookmarks').insert({user_id:user.id,playlist_video_id:v.id,position_seconds:pos,label:'Saved moment'});loadBookmarks()}async function loadTranscript(){const{data}=await supabase.from('video_transcripts').select('*').eq('playlist_video_id',v.id).maybeSingle();setTranscript(data||null)}async function generate(){setTranscript({status:'processing'});const r=await supabase.functions.invoke('get-youtube-transcript',{body:{playlist_video_id:v.id}});if(r.error)setTranscript({status:'error',error:r.error.message});else setTranscript(r.data)}if(!p||!v)return null;const percent=Math.min(100,Math.round((watched/Math.max(progress[v.id]?.duration_seconds||1,1))*100));return <div className="page"><button className="back" onClick={back}>← Back to course</button><section className="player-layout"><main className="player-card"><div className="player-frame"><div id="yt-player"/></div><div className="watch-track"><i style={{width:percent+'%'}}/></div><div className="player-info"><p className="eyebrow">{p.title}</p><h1>{v.title}</h1><p className="muted">{percent}% watched · {time(watched)} {progress[v.id]?.completed_at?'· ✓ Completed':''}</p><div className="player-actions"><button onClick={bookmark}>🔖 Bookmark moment</button><div className="course-tabs">{['outline','notes','transcript','bookmarks'].map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</div></div>{tab==='outline'&&<Outline chapters={ch} player={player}/>} {tab==='notes'&&<div className="section"><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Add a timestamped note for your team…"/><button onClick={saveNote}>Post note at current time</button>{notes.map(n=><button className="note-card" key={n.id} onClick={()=>player.current?.seekTo(n.position_seconds||0,true)}><b>{time(n.position_seconds)}</b> · {n.content}</button>)}</div>} {tab==='transcript'&&<div className="section">{!transcript&&<><p className="muted">Generate a timestamped transcript for this video.</p><button onClick={generate}>Generate transcript</button></>}{transcript?.status==='processing'&&<p>Generating transcript…</p>}{transcript?.status==='error'&&<><p className="error">{transcript.error}</p><button onClick={generate}>Retry</button></>}{transcript?.status==='ready'&&<div className="transcript-list">{(transcript.segments||[]).map((s,i)=><button key={i} onClick={()=>player.current?.seekTo(Number(s.start||0),true)}><span>{time(s.start)}</span>{s.text}</button>)}</div>}</div>}{tab==='bookmarks'&&<div className="section">{bookmarks.length?bookmarks.map(b=><button className="note-card" key={b.id} onClick={()=>player.current?.seekTo(b.position_seconds,true)}>🔖 {time(b.position_seconds)} · {b.label||'Saved moment'}</button>):<p className="muted">No bookmarks in this video yet.</p>}</div>}</div></main><aside className="course-sidebar"><div className="eyebrow">UP NEXT</div>{vs.map((x,i)=><button className={x.id===v.id?'side-video active':''} key={x.id} onClick={()=>window.dispatchEvent(new CustomEvent('learning:open-video',{detail:x.id}))}><span>{progress[x.id]?.completed_at?'✓':' '+(i+1)}</span><b>{x.title}</b></button>)}</aside></section></div>}
function Outline({chapters,player}){return <div className="section">{chapters.length?chapters.map(x=><button className="chapter-row" key={x.id} onClick={()=>player.current?.seekTo(Number(x.start_seconds),true)}><span>{time(x.start_seconds)}</span><b>{x.title}</b></button>):<p className="muted">No chapters available for this video.</p>}</div>}