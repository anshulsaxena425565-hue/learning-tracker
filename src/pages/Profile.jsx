import{useEffect,useState}from'react';import{useAuth}from'../context/AuthContext';import{supabase}from'../lib/supabase';export default function Profile(){const{user}=useAuth(),[p,setP]=useState(null),[form,setForm]=useState({});useEffect(()=>{supabase.from('profiles').select('*').eq('id',user.id).maybeSingle().then(({data})=>{setP(data);setForm(data||{})})},[user]);async function save(){const{data,error}=await supabase.from('profiles').update({display_name:form.display_name,learning_goal:form.learning_goal,bio:form.bio,avatar_url:form.avatar_url||null,theme:form.theme||'system',updated_at:new Date().toISOString()}).eq('id',user.id).select().single();if(error)alert(error.message);else setP(data)}if(!p)return <div className="section">Loading profile…</div>;return <div className="page"><header><div><p className="eyebrow">PROFILE</p><h1>Your learning identity</h1></div></header><section className="section profile-form"><label>Display name<input value={form.display_name||''} onChange={e=>setForm({...form,display_name:e.target.value})}/></label><label>Learning goal<input value={form.learning_goal||''} onChange={e=>setForm({...form,learning_goal:e.target.value})}/></label><label>About<textarea value={form.bio||''} onChange={e=>setForm({...form,bio:e.target.value})}/></label><label>Avatar URL<input value={form.avatar_url||''} onChange={e=>setForm({...form,avatar_url:e.target.value})}/></label><label>Theme<select value={form.theme||'system'} onChange={e=>setForm({...form,theme:e.target.value})}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label><button onClick={save}>Save changes</button></section></div>