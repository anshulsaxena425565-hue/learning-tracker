import{useEffect,useState}from'react';import{useAuth}from'../context/AuthContext';import{supabase}from'../lib/supabase';export default function Profile(){const{user}=useAuth(),[p,setP]=useState(null),[form,setForm]=useState({});useEffect(()=>{supabase.from('profiles').select('*').eq('id',user.id).maybeSingle().then(({data})=>{setP(data);setForm(data||{})})},[user]);async function save(){const theme=form.theme||'system';document.documentElement.dataset.theme=theme==='system'?(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'):theme;localStorage.setItem('lb-theme',theme);const{data,error}=await supabase.from('profiles').update({display_name:form.display_name,learning_goal:form.learning_goal,bio:form.bio,avatar_url:form.avatar_url||null,theme,updated_at:new Date().toISOString()}).eq('id',user.id).select().single();if(error)alert(error.message);else setP(data)}if(!p)return <div className="section">Loading profile…</div>;const name=form.display_name||'Learner',initials=name.split(/\\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase();return <div className="page"><header><div><p className="eyebrow">PROFILE</p><h1>Your learning identity</h1><p className="muted">Shape how your learning identity appears to teammates.</p></div></header><section className="profile-shell"><div className="profile-hero"><div className="profile-avatar">{form.avatar_url?<img src={form.avatar_url} alt=""/>:initials}</div><div className="grow"><h2>{name}</h2><p className="muted">{form.learning_goal||'Set a learning goal to personalize your profile.'}</p></div><span className="pill">Learner</span></div><div className="profile-body"><section><p className="eyebrow">PERSONAL DETAILS</p><div className="profile-field"><label>Display name</label><input value={form.display_name||''} onChange={e=>setForm({...form,display_name:e.target.value})} placeholder="Your name"/></div><div className="profile-field"><label>Learning goal</label><input value={form.learning_goal||''} onChange={e=>setForm({...form,learning_goal:e.target.value})} placeholder="e.g. Become a Spring Boot developer"/></div><div className="profile-field"><label>About</label><textarea value={form.bio||''} onChange={e=>setForm({...form,bio:e.target.value})} placeholder="A short intro for your teammates…"/></div></section><aside className="profile-side"><p className="eyebrow">PROFILE APPEARANCE</p><div className="profile-preview"><div className="profile-avatar small">{form.avatar_url?<img src={form.avatar_url} alt=""/>:initials}</div><div className="grow"><b>{name}</b><small>{form.learning_goal||'Your learning goal'}</small></div></div><label>Profile picture URL<input value={form.avatar_url||''} onChange={e=>setForm({...form,avatar_url:e.target.value})} placeholder="https://…"/></label><label>Theme<select value={form.theme||'system'} onChange={e=>setForm({...form,theme:e.target.value})}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label></aside></div><div className="profile-actions"><button className="ghost" onClick={()=>setForm(p||{})}>Cancel</button><button onClick={save}>Save changes</button></div></section></div>}