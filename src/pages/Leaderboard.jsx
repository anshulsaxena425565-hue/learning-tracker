import{useMemo}from'react';import{useTeam}from'../context/TeamContext';export default function Leaderboard(){const{team,members,memberProgress,videos}=useTeam();const rows=useMemo(()=>{const total=Object.values(videos).flat().length;return members.map(m=>({m,done:memberProgress[m.user_id]?.size||0,total,percent:Math.round((memberProgress[m.user_id]?.size||0)/(total||1)*100)})).sort((a,b)=>b.done-a.done)},[members,memberProgress,videos]);return <div className="page"><header><div><p className="eyebrow">TEAM PROGRESS</p><h1>Leaderboard</h1><p className="muted">{team?.name} · completed lessons</p></div></header><section className="section"><table><thead><tr><th>#</th><th>Learner</th><th>Progress</th><th>Done</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.m.user_id}><td>{i+1}</td><td>{r.m.profiles?.display_name||'Member'}</td><td><div className="progress"><i style={{width:r.percent+'%'}}/></div><small>{r.percent}%</small></td><td>{r.done}</td></tr>)}</tbody></table></section></div>}