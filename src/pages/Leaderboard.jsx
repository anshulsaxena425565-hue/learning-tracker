import{useEffect,useMemo,useState}from'react';import{supabase}from'../lib/supabase';import{useTeam}from'../context/TeamContext';import{useAuth}from'../context/AuthContext';

function Icon({type}){const paths={users:<><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0 1 12 0"/><path d="M16 5a3 3 0 0 1 0 6"/><path d="M17 14a5 5 0 0 1 4 6"/></>,book:<><rect x="5" y="3" width="14" height="18" rx="2"/><path d="M9 3v18M9 7h6"/></>,trend:<><path d="M4 17l5-5 4 3 7-9"/><path d="M15 6h5v5"/></>,flame:<><path d="M12 21c4.4 0 7-2.8 7-6.5 0-3.4-2.2-5.5-4.8-8.5.1 2.4-.8 3.7-2 4.7.2-3.7-1.8-6.4-4.2-8.7.2 4.2-3 6.4-3 10.2C5 17.5 7.9 21 12 21z"/></>,chart:<><path d="M5 20V10M12 20V5M19 20v-8"/></>,calendar:<><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></>,trophy:<><path d="M8 4h8v4a4 4 0 0 1-8 0z"/><path d="M8 5H5v2a4 4 0 0 0 4 4M16 5h3v2a4 4 0 0 1-4 4"/><path d="M12 12v4M8 20h8M9 16h6"/></>,quiz:<><path d="M6 4h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/><path d="M8 8h8M8 12h5M8 16h7"/></>,bolt:<path d="M13 2L4 14h7l-1 8 10-13h-7z"/>,target:<><circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="4"/><circle cx="12" cy="12" r="1"/></>,medal:<><circle cx="12" cy="15" r="6"/><path d="M8 9l-2-6h4l2 5 2-5h4l-2 6"/></>};return <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">{paths[type]}</svg>}

function since(period){const now=Date.now();if(period==='week')return now-7*864e5;if(period==='month')return now-30*864e5;return 0}
function streak(activity){const days=[...new Set((activity?.completed||[]).map(x=>new Date(x.completedAt).toISOString().slice(0,10)))].sort().reverse();if(!days.length)return 0;let count=0,d=new Date(days[0]+'T00:00:00');for(const day of days){const cur=new Date(day+'T00:00:00');if(Math.round((d-cur)/864e5)===0){count++;d=new Date(cur.getTime()-864e5)}else break}return count}
function initials(name){return String(name||'Member').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase()}

export default function Leaderboard(){
 const{user}=useAuth(),{team,members,memberProgress,memberActivity,videos}=useTeam(),[tab,setTab]=useState('personal'),[period,setPeriod]=useState('all'),[quizAttempts,setQuizAttempts]=useState([]),[selected,setSelected]=useState(null);
 const totalLessons=Object.values(videos).flat().length;
 const admin=['owner','admin'].includes(team?.role);
 useEffect(()=>{(async()=>{if(!team?.id)return;const tests=(await supabase.from('test_series').select('id').eq('team_id',team.id)).data||[];const chunks=await Promise.all(tests.map(t=>supabase.rpc('get_test_attempt_history',{p_test_id:t.id})));setQuizAttempts(chunks.flatMap(x=>x.data||[]))})()},[team?.id]);
 const cutoff=period==='week'?Date.now()-7*864e5:period==='month'?Date.now()-30*864e5:0;
 const rows=useMemo(()=>members.map(m=>{const a=memberActivity[m.user_id]||{completed:[],inProgress:[],timeSeconds:0},done=(a.completed||[]).filter(x=>!cutoff||new Date(x.completedAt).getTime()>=cutoff).length,all=memberProgress[m.user_id]?.size||0,q=quizAttempts.filter(x=>x.user_id===m.user_id),avg=q.length?Math.round(q.reduce((n,x)=>n+Number(x.percentage||0),0)/q.length):0,time=Number(a.timeSeconds||0),xp=all*50+Math.floor(time/300)*5+q.reduce((n,x)=>n+Math.round(Number(x.percentage||0)/10),0);return{m,a,done,all,quiz:q.length,avg,time,xp,streak:streak(a),percent:Math.round(all/(totalLessons||1)*100)}}),[members,memberActivity,memberProgress,quizAttempts,totalLessons,cutoff]);
 const personal=rows.find(r=>r.m.user_id===user?.id)||{done:0,all:0,quiz:0,avg:0,time:0,xp:0,streak:0,percent:0,m:{profiles:{display_name:'You'}}};
 const totalXp=rows.reduce((n,r)=>n+r.xp,0),teamTime=rows.reduce((n,r)=>n+r.time,0),teamDone=rows.reduce((n,r)=>n+r.all,0),teamQuiz=rows.reduce((n,r)=>n+r.quiz,0);
 const fmt=s=>{s=Math.max(0,Math.floor(s||0));const h=Math.floor(s/3600),m=Math.floor(s%3600/60);return h?(h+'h '+m+'m'):(m+'m')};
 const sorted=[...rows].sort((a,b)=>b.xp-a.xp);
 return <div className="page analytics-page">
  <header className="analytics-hero-head"><div><p className="eyebrow">LEARNING INTELLIGENCE</p><h1>Leaderboard <span>& insights</span></h1><p className="muted">Turn learning activity into clear progress, momentum and meaningful milestones.</p></div><select value={period} onChange={e=>setPeriod(e.target.value)}><option value="all">All time</option><option value="month">This month</option><option value="week">This week</option></select></header>
  <div className="analytics-tabs"><button className={tab==='personal'?'active':''} onClick={()=>setTab('personal')}>✨ My insights</button><button className={tab==='team'?'active':''} onClick={()=>setTab('team')}>🏆 Team analytics</button>{admin&&<button className={tab==='admin'?'active':''} onClick={()=>setTab('admin')}>🛡 Admin view</button>}</div>
  {tab==='personal'&&<><section className="analytics-summary-grid"><article className="analytics-main-card"><p className="eyebrow">YOUR LEARNING LEVEL</p><div className="level-row"><div className="level-orb"><b>{Math.max(1,Math.floor(personal.xp/250)+1)}</b><small>LEVEL</small></div><div><h2>{personal.xp} XP</h2><p>{personal.streak?'🔥 '+personal.streak+' day streak — keep the momentum.':'Start a learning streak and build your momentum.'}</p><div className="xp-progress"><i style={{width:Math.min(100,(personal.xp%250)/250*100)+'%'}}/></div><small>{250-(personal.xp%250)} XP to next level</small></div></div></article><Metric icon="clock" value={fmt(personal.time)} label="Time learned"/><Metric icon="chart" value={personal.percent+'%'} label="Topics / lessons done"/><Metric icon="quiz" value={personal.quiz} label="Quizzes solved"/><Metric icon="bolt" value={personal.avg+'%'} label="Quiz average"/></section><section className="analytics-lower-grid"><InsightCard title="Your learning snapshot"><Insight label="Lessons completed" value={personal.all}/><Insight label="Current streak" value={personal.streak+' days'}/><Insight label="XP gained" value={personal.xp}/><Insight label="Quiz accuracy" value={personal.avg+'%'}/></InsightCard><section className="analytics-card"><div className="analytics-card-head"><div><p className="eyebrow">MOMENTUM</p><h3>Keep your quest moving</h3></div><span>✦</span></div><div className="momentum-visual"><div className="momentum-ring" style={{'--p':personal.percent+'%'}}><b>{personal.percent}%</b><small>complete</small></div><div><b>{personal.all} lessons</b><p>{personal.time?fmt(personal.time)+' invested in learning.':'Your learning time will appear here.'}</p><strong>{personal.xp} XP earned</strong></div></div></section></section></>}
  {tab==='team'&&<><section className="analytics-summary-grid"><Metric icon="users" value={rows.length} label="Team members"/><Metric icon="clock" value={fmt(teamTime)} label="Team learning time"/><Metric icon="chart" value={teamDone} label="Lessons completed"/><Metric icon="quiz" value={teamQuiz} label="Quiz attempts"/><Metric icon="bolt" value={totalXp} label="Team XP"/></section><section className="analytics-card"><div className="analytics-card-head"><div><p className="eyebrow">TEAM LEADERBOARD</p><h2>Learning arena</h2><span>XP combines completed lessons, learning time and quiz activity.</span></div></div><div className="analytics-table-head"><span>#</span><span>Learner</span><span>Time learned</span><span>Topics</span><span>Quizzes</span><span>XP</span></div>{sorted.map((r,i)=><button className="analytics-row" key={r.m.user_id} onClick={()=>setSelected(r)}><b>{i+1}</b><span className="analytics-person"><i>{initials(r.m.profiles?.display_name)}</i><strong>{r.m.profiles?.display_name||'Member'}</strong></span><span>{fmt(r.time)}</span><span>{r.all}</span><span>{r.quiz}</span><span className="xp-value">✦ {r.xp}</span></button>)}</section></>}
  {tab==='admin'&&admin&&<><section className="analytics-summary-grid admin-summary"><Metric icon="users" value={rows.length} label="Learners"/><Metric icon="clock" value={fmt(teamTime)} label="Total time learned"/><Metric icon="chart" value={teamDone} label="Topics completed"/><Metric icon="quiz" value={teamQuiz} label="Quiz attempts"/><Metric icon="bolt" value={totalXp} label="XP generated"/></section><section className="admin-insight-grid"><InsightCard title="Overall team insights"><Insight label="Average completion" value={Math.round(rows.reduce((n,r)=>n+r.percent,0)/(rows.length||1))+'%'}/><Insight label="Average quiz score" value={Math.round(rows.reduce((n,r)=>n+r.avg,0)/(rows.length||1))+'%'}/><Insight label="Active streak" value={Math.max(0,...rows.map(r=>r.streak))+' days'}/><Insight label="Total learning time" value={fmt(teamTime)}/><Insight label="Total XP" value={totalXp}/></InsightCard><section className="analytics-card admin-learners"><div className="analytics-card-head"><div><p className="eyebrow">DETAILED LEARNER VIEW</p><h3>Every learner</h3><span>Select anyone to inspect their learning insights.</span></div></div>{rows.map(r=><button className="admin-learner-row" key={r.m.user_id} onClick={()=>setSelected(r)}><i>{initials(r.m.profiles?.display_name)}</i><div><b>{r.m.profiles?.display_name||'Member'}</b><small>{r.all} topics · {fmt(r.time)} · {r.quiz} quizzes · {r.streak} day streak</small></div><strong>✦ {r.xp} XP</strong></button>)}</section></section></>}
  {selected&&<div className="analytics-modal-backdrop" onClick={()=>setSelected(null)}><div className="analytics-modal" onClick={e=>e.stopPropagation()}><button onClick={()=>setSelected(null)}>×</button><div className="modal-profile"><i>{initials(selected.m.profiles?.display_name)}</i><div><p className="eyebrow">LEARNER INSIGHTS</p><h2>{selected.m.profiles?.display_name||'Member'}</h2><span>{selected.m.profiles?.learning_goal||'Learning journey'}</span></div></div><div className="modal-metrics"><Metric icon="clock" value={fmt(selected.time)} label="Time learned"/><Metric icon="chart" value={selected.all} label="Topics done"/><Metric icon="quiz" value={selected.quiz} label="Quizzes solved"/><Metric icon="bolt" value={selected.xp} label="XP gained"/><Metric icon="flame" value={selected.streak+'d'} label="Streak"/></div><div className="modal-note">Detailed learner analytics are based on recorded course progress and submitted quiz activity.</div></div></div>}
 </div>
}
function Metric({icon,value,label}){return <article className="analytics-metric"><span><Icon type={icon}/></span><b>{value}</b><small>{label}</small></article>}
function InsightCard({title,children}){return <section className="analytics-card"><div className="analytics-card-head"><div><p className="eyebrow">INSIGHTS</p><h3>{title}</h3></div></div><div className="insight-list">{children}</div></section>}
function Insight({label,value}){return <div><span>{label}</span><b>{value}</b></div>}