import{useMemo}from'react';import{useTeam}from'../context/TeamContext';export default function Leaderboard(){const{team,members,memberProgress,videos}=useTeam();const rows=useMemo(()=>{const total=Object.values(videos).flat().length;return members.map(m=>({m,done:memberProgress[m.user_id]?.size||0,total,percent:Math.round((memberProgress[m.user_id]?.size||0)/(total||1)*100)})).sort((a,b)=>b.done-a.done)},[members,memberProgress,videos]);return <div className="page"><header><div><p className="eyebrow">TEAM PROGRESS</p><h1>Learning leaderboard</h1><p className="muted">{team?.name} · see how the team is progressing.</p></div></header><section className="section"><div className="leaderboard-head"><div><h2>Team activity</h2><p className="muted">Completed lessons across this workspace.</p></div><span className="pill">{rows.length} learners</span></div><div className="leaderboard-list">{rows.map((r,i)=><div className={'leader-row '+(r.m.user_id===team?.created_by?'owner':'')} key={r.m.user_id}><div className="rank">{i<3?['🥇','🥈','🥉'][i]:i+1}</div><div className="avatar-dot">{(r.m.profiles?.display_name||'M')[0].toUpperCase()}</div><div className="grow"><b>{r.m.profiles?.display_name||'Member'}</b><div className="meta">{r.done} lessons completed</div><div className="progress mini"><i style={{width:r.percent+'%'}}/></div></div><strong>{r.percent}%</strong></div>)}</div></section></div>}