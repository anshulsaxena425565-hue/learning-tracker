import{useMemo,useState}from'react';import{useTeam}from'../context/TeamContext';
function Icon({type}){const paths={users:<><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0 1 12 0"/><path d="M16 5a3 3 0 0 1 0 6"/><path d="M17 14a5 5 0 0 1 4 6"/></>,book:<><rect x="5" y="3" width="14" height="18" rx="2"/><path d="M9 3v18M9 7h6"/></>,trend:<><path d="M4 17l5-5 4 3 7-9"/><path d="M15 6h5v5"/></>,flame:<><path d="M12 21c4.4 0 7-2.8 7-6.5 0-3.4-2.2-5.5-4.8-8.5.1 2.4-.8 3.7-2 4.7.2-3.7-1.8-6.4-4.2-8.7.2 4.2-3 6.4-3 10.2C5 17.5 7.9 21 12 21z"/></>,chart:<><path d="M5 20V10M12 20V5M19 20v-8"/></>,calendar:<><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></>,trophy:<><path d="M8 4h8v4a4 4 0 0 1-8 0z"/><path d="M8 5H5v2a4 4 0 0 0 4 4M16 5h3v2a4 4 0 0 1-4 4"/><path d="M12 12v4M8 20h8M9 16h6"/></>};return <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">{paths[type]}</svg>}
function since(period){const now=Date.now();if(period==='week')return now-7*864e5;if(period==='month')return now-30*864e5;return 0}
export default function Leaderboard(){const{team,members,memberProgress,memberActivity,videos}=useTeam();const[period,setPeriod]=useState('month');const[metric,setMetric]=useState('lessons');const totalLessons=Object.values(videos).flat().length;const rows=useMemo(()=>{const cutoff=since(period);return members.map(m=>{const a=memberActivity[m.user_id]||{completed:[],inProgress:[]};const completed=a.completed.filter(x=>!cutoff||new Date(x.completedAt).getTime()>=cutoff);const allDone=memberProgress[m.user_id]?.size||0;const done=period==='all'?allDone:completed.length;const inProgress=a.inProgress.length;return{m,done,allDone,inProgress,percent:Math.round(done/(totalLessons||1)*100),recent:completed.length}}).sort((a,b)=>metric==='lessons'?b.done-a.done:b.percent-a.percent)},[members,memberActivity,memberProgress,videos,period,metric,totalLessons]);const totalDone=rows.reduce((n,r)=>n+r.done,0);const avg=rows.length?Math.round(rows.reduce((n,r)=>n+r.percent,0)/rows.length):0;const active=rows.filter(r=>r.recent>0||r.inProgress>0).length;const inProgress=rows.reduce((n,r)=>n+r.inProgress,0);const notStarted=Math.max(0,totalLessons*rows.length-totalDone-inProgress);const periodLabel=period==='all'?'All time':period==='week'?'This week':'This month';async function invite(){const link=team?.code?window.location.origin+window.location.pathname+'?join='+team.code:window.location.href;try{if(navigator.share)await navigator.share({title:'Join my LearningBeyond team',text:'Join my learning workspace',url:link});else{await navigator.clipboard.writeText(link);alert('Invite link copied')}}catch{}}
return <div className="page leaderboard-page">
<header className="leaderboard-hero"><div><div className="title-kicker"><span className="title-icon"><Icon type="users"/></span> LEADERBOARD</div><h1>Learning <span>leaderboard</span></h1><p className="muted">See how your team is progressing.</p></div><div className="leaderboard-date"><Icon type="calendar"/><select value={period} onChange={e=>setPeriod(e.target.value)}><option value="week">This week</option><option value="month">This month</option><option value="all">All time</option></select></div><div className="trophy-art" aria-hidden="true"><Icon type="trophy"/></div></header>
<section className="leader-kpis"><article><div className="stat-icon purple"><Icon type="users"/></div><div><strong>{rows.length}</strong><span>Team members</span></div><em className="trend-up">● {active} active</em></article><article><div className="stat-icon blue"><Icon type="book"/></div><div><strong>{totalDone}</strong><span>Lessons completed</span></div><em className="trend-neutral">{periodLabel}</em></article><article><div className="stat-icon green"><Icon type="trend"/></div><div><strong>{avg}%</strong><span>Average completion</span></div><em className="trend-neutral">Across {rows.length} learners</em></article><article><div className="stat-icon orange"><Icon type="flame"/></div><div><strong>{inProgress}</strong><span>Lessons in progress</span></div><em className="trend-warm">{inProgress?'Keep the momentum':'Start today'}</em></article></section>
<section className="leaderboard-panel"><div className="leaderboard-panel-head"><div className="panel-title"><span className="panel-icon"><Icon type="users"/></span><div><h2>Team leaderboard</h2><p>{periodLabel} · completed lessons across this workspace.</p></div></div><div className="segmented"><button className={metric==='lessons'?'active':''} onClick={()=>setMetric('lessons')}>By lessons</button><button className={metric==='completion'?'active':''} onClick={()=>setMetric('completion')}>By completion</button></div></div><div className="leader-table-head"><span>#</span><span>Member</span><span>Lessons completed</span><span>Completion</span><span></span></div><div className="leader-table">{rows.map((r,i)=><article className="leaderboard-row-modern" key={r.m.user_id}><div className={'rank-medal rank-'+(i+1)}>{i+1}</div><div className="member-cell"><span className="member-avatar">{(r.m.profiles?.display_name||'M')[0].toUpperCase()}</span><div><b>{r.m.profiles?.display_name||'Member'}</b><small>{r.m.profiles?.learning_goal||'Learner'}{r.inProgress?' · '+r.inProgress+' in progress':''}</small></div></div><div className="lesson-cell"><div className="leader-track"><i style={{width:r.percent+'%'}}/></div><span>{r.done}</span></div><div className="completion-cell"><strong>{r.percent}%</strong><span className="mini-ring" style={{'--pct':r.percent+'%'}}/></div><span className="row-status">{r.recent?'Active':''}</span></article>)}</div>{!rows.length&&<div className="empty">No learners in this workspace yet.</div>}</section>
<div className="leader-bottom"><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type="chart"/></span><h3>Top progress · {periodLabel.toLowerCase()}</h3></div></div><div className="mini-ranking">{rows.slice(0,3).map((r,i)=><div className="mini-rank-row" key={r.m.user_id}><span>{i+1}</span><b>{r.m.profiles?.display_name||'Member'}</b><div><i style={{width:r.percent+'%'}}/></div><strong>{r.percent}%</strong></div>)}{!rows.length&&<div className="empty-state"><b>No learners yet</b><p>Progress will appear here as your team learns.</p></div>}</div></section><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type="trend"/></span><h3>Learning distribution</h3></div></div><div className="distribution"><div className="distribution-ring" style={{'--pct':totalLessons&&rows.length?Math.round(totalDone/(totalLessons*rows.length)*100)+'%':'0%'}}><strong>{totalDone}</strong><small>completed</small></div><div className="distribution-legend"><div><i className="dot completed"/>Completed <b>{totalDone}</b></div><div><i className="dot progress-dot"/>In progress <b>{inProgress}</b></div><div><i className="dot not-started"/>Not started <b>{notStarted}</b></div></div></div></section></div>
<section className="invite-strip"><div><span className="invite-icon"><Icon type="users"/></span><div><h3>Learning is more fun together!</h3><p>Invite your team, take courses and climb the leaderboard.</p></div></div><button onClick={invite}>Invite teammates <span>→</span></button></section>
</div>}