import{useEffect,useMemo,useState}from'react';import{supabase}from'../lib/supabase';import{useTeam}from'../context/TeamContext';

function Icon({type}){const paths={users:<><circle cx="9" cy="8" r="3"/><path d="M3 20a6 6 0 0 1 12 0"/><path d="M16 5a3 3 0 0 1 0 6"/><path d="M17 14a5 5 0 0 1 4 6"/></>,book:<><rect x="5" y="3" width="14" height="18" rx="2"/><path d="M9 3v18M9 7h6"/></>,trend:<><path d="M4 17l5-5 4 3 7-9"/><path d="M15 6h5v5"/></>,flame:<><path d="M12 21c4.4 0 7-2.8 7-6.5 0-3.4-2.2-5.5-4.8-8.5.1 2.4-.8 3.7-2 4.7.2-3.7-1.8-6.4-4.2-8.7.2 4.2-3 6.4-3 10.2C5 17.5 7.9 21 12 21z"/></>,chart:<><path d="M5 20V10M12 20V5M19 20v-8"/></>,calendar:<><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></>,trophy:<><path d="M8 4h8v4a4 4 0 0 1-8 0z"/><path d="M8 5H5v2a4 4 0 0 0 4 4M16 5h3v2a4 4 0 0 1-4 4"/><path d="M12 12v4M8 20h8M9 16h6"/></>,quiz:<><path d="M6 4h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/><path d="M8 8h8M8 12h5M8 16h7"/></>,bolt:<path d="M13 2L4 14h7l-1 8 10-13h-7z"/>,target:<><circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="4"/><circle cx="12" cy="12" r="1"/></>,medal:<><circle cx="12" cy="15" r="6"/><path d="M8 9l-2-6h4l2 5 2-5h4l-2 6"/></>};return <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">{paths[type]}</svg>}

function since(period){const now=Date.now();if(period==='week')return now-7*864e5;if(period==='month')return now-30*864e5;return 0}
function streak(activity){const days=[...new Set((activity?.completed||[]).map(x=>new Date(x.completedAt).toISOString().slice(0,10)))].sort().reverse();if(!days.length)return 0;let count=0,d=new Date(days[0]+'T00:00:00');for(const day of days){const cur=new Date(day+'T00:00:00');if(Math.round((d-cur)/864e5)===0){count++;d=new Date(cur.getTime()-864e5)}else break}return count}
function initials(name){return String(name||'Member').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase()}

export default function Leaderboard(){
 const{team,members,memberProgress,memberActivity,videos}=useTeam();
 const[period,setPeriod]=useState('month'),[metric,setMetric]=useState('xp'),[tab,setTab]=useState('overview'),[quizStats,setQuizStats]=useState({tests:[],attempts:[],loading:true}),[selected,setSelected]=useState(null);
 const totalLessons=Object.values(videos).flat().length;
 useEffect(()=>{let alive=true;async function load(){if(!team?.id){setQuizStats({tests:[],attempts:[],loading:false});return}setQuizStats(x=>({...x,loading:true}));const{data:tests}=await supabase.from('test_series').select('id,title,playlist_id,created_at,max_reattempts,time_limit_minutes').eq('team_id',team.id).order('created_at',{ascending:false});const list=tests||[];const chunks=await Promise.all(list.slice(0,20).map(async t=>{const{data,error}=await supabase.rpc('get_test_attempt_history',{p_test_id:t.id});return{test:t,attempts:error?[]:(data||[])}}));if(alive)setQuizStats({tests:list,attempts:chunks.flatMap(x=>x.attempts.map(a=>({...a,test_id:x.test.id,test_title:x.test.title}))),loading:false})}load();return()=>{alive=false}},[team?.id]);
 const cutoff=since(period);
 const rows=useMemo(()=>members.map(m=>{const a=memberActivity[m.user_id]||{completed:[],inProgress:[]};const completed=a.completed.filter(x=>!cutoff||new Date(x.completedAt).getTime()>=cutoff);const allDone=memberProgress[m.user_id]?.size||0;const done=period==='all'?allDone:completed.length;const q=quizStats.attempts.filter(x=>x.user_id===m.user_id);const qAvg=q.length?Math.round(q.reduce((n,x)=>n+Number(x.percentage||0),0)/q.length):0;const best=q.length?Math.max(...q.map(x=>Number(x.percentage||0))):0;const xp=done*20+q.reduce((n,x)=>n+Math.round(Number(x.percentage||0)/10),0);return{m,done,allDone,inProgress:a.inProgress.length,percent:Math.round(done/(totalLessons||1)*100),recent:completed.length,quizAttempts:q.length,quizAvg:qAvg,quizBest:best,xp,streak:streak(a)}}).sort((a,b)=>metric==='completion'?b.percent-a.percent:metric==='quiz'?b.quizAvg-a.quizAvg:metric==='streak'?b.streak-a.streak:b.xp-a.xp),[members,memberActivity,memberProgress,videos,period,metric,totalLessons,quizStats.attempts]);
 const totalDone=rows.reduce((n,r)=>n+r.done,0),avg=rows.length?Math.round(rows.reduce((n,r)=>n+r.percent,0)/rows.length):0,active=rows.filter(r=>r.recent>0||r.inProgress>0||r.quizAttempts>0).length,inProgress=rows.reduce((n,r)=>n+r.inProgress,0);
 const quizAttempts=quizStats.attempts.length,quizAvg=quizAttempts?Math.round(quizStats.attempts.reduce((n,x)=>n+Number(x.percentage||0),0)/quizAttempts):0,quizBest=quizAttempts?Math.max(...quizStats.attempts.map(x=>Number(x.percentage||0))):0,totalXp=rows.reduce((n,r)=>n+r.xp,0);
 const topQuiz=useMemo(()=>{const map={};quizStats.attempts.forEach(a=>{const id=a.user_id;if(!map[id])map[id]={user_id:id,name:a.display_name||'Member',attempts:0,avg:0,best:0};map[id].attempts++;map[id].avg+=Number(a.percentage||0);map[id].best=Math.max(map[id].best,Number(a.percentage||0))});return Object.values(map).map(x=>({...x,avg:Math.round(x.avg/x.attempts)})).sort((a,b)=>b.avg-a.avg)},[quizStats.attempts]);
 const achievements=useMemo(()=>{const out=[];rows.forEach(r=>{const name=r.m.profiles?.display_name||'Member';if(r.done>=10)out.push({user_id:r.m.user_id,name,icon:'🏆',title:'Course Climber',detail:r.done+' lessons completed'});if(r.quizBest>=90)out.push({user_id:r.m.user_id,name,icon:'🎯',title:'Quiz Ace',detail:r.quizBest+'% best quiz score'});if(r.streak>=3)out.push({user_id:r.m.user_id,name,icon:'🔥',title:'On Fire',detail:r.streak+' day learning streak'});if(r.xp>=200)out.push({user_id:r.m.user_id,name,icon:'⚡',title:'XP Hunter',detail:r.xp+' learning XP'});});return out.slice(0,12)},[rows]);
 const periodLabel=period==='all'?'All time':period==='week'?'This week':'This month';
 async function invite(){const link=team?.code?window.location.origin+window.location.pathname+'?join='+team.code:window.location.href;try{if(navigator.share)await navigator.share({title:'Join my LearningBeyond team',text:'Join my learning workspace',url:link});else{await navigator.clipboard.writeText(link);alert('Invite link copied')}}catch{}}
 return <div className="page leaderboard-page">
  <header className="leaderboard-hero"><div><div className="title-kicker"><span className="title-icon"><Icon type="trophy"/></span> TEAM PERFORMANCE</div><h1>Learning <span>arena</span></h1><p className="muted">Progress, quizzes, streaks, XP and milestones — all in one place.</p></div><div className="leaderboard-date"><Icon type="calendar"/><select value={period} onChange={e=>setPeriod(e.target.value)}><option value="week">This week</option><option value="month">This month</option><option value="all">All time</option></select></div><div className="trophy-art"><Icon type="trophy"/></div></header>
  <div className="leader-tabs"><button className={tab==='overview'?'active':''} onClick={()=>setTab('overview')}>🏆 Overview</button><button className={tab==='quizzes'?'active':''} onClick={()=>setTab('quizzes')}>🧠 Quiz analytics</button><button className={tab==='achievements'?'active':''} onClick={()=>setTab('achievements')}>✨ Achievements</button></div>
  {tab==='overview'&&<><section className="leader-kpis"><article><div className="stat-icon purple"><Icon type="users"/></div><div><strong>{rows.length}</strong><span>Team members</span></div><em className="trend-up">● {active} active</em></article><article><div className="stat-icon blue"><Icon type="bolt"/></div><div><strong>{totalXp}</strong><span>Learning XP</span></div><em className="trend-neutral">20 XP / lesson + quiz bonus</em></article><article><div className="stat-icon green"><Icon type="target"/></div><div><strong>{avg}%</strong><span>Course completion</span></div><em className="trend-neutral">{totalDone} lessons · {periodLabel}</em></article><article><div className="stat-icon orange"><Icon type="quiz"/></div><div><strong>{quizAvg}%</strong><span>Quiz accuracy</span></div><em className="trend-warm">{quizAttempts} recorded attempts</em></article></section>
  <section className="leaderboard-panel"><div className="leaderboard-panel-head"><div className="panel-title"><span className="panel-icon"><Icon type="medal"/></span><div><h2>Team leaderboard</h2><p>Compete on learning progress, quiz performance and consistency.</p></div></div><div className="segmented"><button className={metric==='xp'?'active':''} onClick={()=>setMetric('xp')}>⚡ XP</button><button className={metric==='completion'?'active':''} onClick={()=>setMetric('completion')}>📚 Completion</button><button className={metric==='quiz'?'active':''} onClick={()=>setMetric('quiz')}>🧠 Quiz</button><button className={metric==='streak'?'active':''} onClick={()=>setMetric('streak')}>🔥 Streak</button></div></div><div className="leader-table-head"><span>#</span><span>Member</span><span>Progress</span><span>Quiz</span><span>XP</span></div><div className="leader-table">{rows.map((r,i)=><article className="leaderboard-row-modern" key={r.m.user_id} onClick={()=>setSelected(r)}><div className={'rank-medal rank-'+(i+1)}>{i<3?['🥇','🥈','🥉'][i]:i+1}</div><div className="member-cell"><span className="member-avatar">{initials(r.m.profiles?.display_name)}</span><div><b>{r.m.profiles?.display_name||'Member'}</b><small>{r.streak?'🔥 '+r.streak+' day streak · ':''}{r.inProgress?r.inProgress+' in progress':r.quizAttempts?r.quizAttempts+' quiz attempts':'Ready to learn'}</small></div></div><div className="lesson-cell"><div className="leader-track"><i style={{width:r.percent+'%'}}/></div><span>{r.percent}%</span></div><div className="quiz-cell"><strong>{r.quizAttempts?r.quizAvg+'%':'—'}</strong><small>{r.quizAttempts?r.quizAttempts+' attempts':'No data'}</small></div><div className="xp-cell"><strong>{r.xp}</strong><small>XP</small></div></article>)}</div>{!rows.length&&<div className="empty">No learners in this workspace yet.</div>}</section>
  <div className="leader-insight-grid"><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type="chart"/></span><h3>Momentum · {periodLabel}</h3></div></div><div className="momentum-list">{rows.slice(0,5).map((r,i)=><div className="momentum-row" key={r.m.user_id}><span>{i+1}</span><b>{r.m.profiles?.display_name||'Member'}</b><div><i style={{width:r.percent+'%'}}/></div><strong>{r.percent}%</strong></div>)}</div></section><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type="trend"/></span><h3>Learning distribution</h3></div></div><div className="distribution"><div className="distribution-ring" style={{'--pct':totalLessons&&rows.length?Math.round(totalDone/(totalLessons*rows.length)*100)+'%':'0%'}}><strong>{totalDone}</strong><small>lessons done</small></div><div className="distribution-legend"><div><i className="dot completed"/>Completed <b>{totalDone}</b></div><div><i className="dot progress-dot"/>In progress <b>{inProgress}</b></div><div><i className="dot not-started"/>Not started <b>{Math.max(0,totalLessons*rows.length-totalDone-inProgress)}</b></div></div></div></section></div>
  <section className="leader-stats-strip"><div><span>🏅 Best quiz score</span><strong>{quizBest||0}%</strong></div><div><span>🔥 Longest active streak</span><strong>{Math.max(0,...rows.map(r=>r.streak))} days</strong></div><div><span>📚 Lessons completed</span><strong>{totalDone}</strong></div><div><span>🧠 Quizzes tracked</span><strong>{quizStats.tests.length}</strong></div></section></>}
  {tab==='quizzes'&&<section className="leader-analytics"><div className="analytics-hero"><div><p className="eyebrow">ASSESSMENT INTELLIGENCE</p><h2>Quiz analytics</h2><p>Understand accuracy, participation, performance and time spent across your team's assessments.</p></div><div className="analytics-score"><strong>{quizAvg||0}%</strong><span>team accuracy</span></div></div><div className="analytics-cards"><article><span>🧠 Quiz tests</span><strong>{quizStats.tests.length}</strong><small>Created in this team</small></article><article><span>📝 Recorded attempts</span><strong>{quizAttempts}</strong><small>Completed submissions</small></article><article><span>🎯 Best score</span><strong>{quizBest||0}%</strong><small>Across recorded attempts</small></article><article><span>⏱ Avg time</span><strong>{quizAttempts?Math.round(quizStats.attempts.reduce((n,x)=>n+Number(x.duration_seconds||0),0)/quizAttempts/60)+'m':'—'}</strong><small>Per completed attempt</small></article></div><div className="analytics-grid"><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type="quiz"/></span><h3>Quiz performance by learner</h3></div></div><div className="quiz-learner-list">{topQuiz.map((q,i)=><div className="quiz-learner-row" key={q.user_id}><span className={'rank-medal rank-'+(i+1)}>{i<3?['🥇','🥈','🥉'][i]:i+1}</span><div className="member-cell"><span className="member-avatar">{initials(q.name)}</span><div><b>{q.name}</b><small>{q.attempts} completed attempts · best {q.best}%</small></div></div><strong>{q.avg}%</strong><div className="leader-track"><i style={{width:q.avg+'%'}}/></div></div>)}{!topQuiz.length&&<div className="empty-state"><b>No quiz data yet</b><p>Complete a quiz to start building analytics.</p></div>}</div></section><section className="leader-info-card"><div className="info-head"><div><span className="panel-icon"><Icon type={quizAttempts?'target':'clock'}/></span><h3>Assessment insights</h3></div></div><div className="insight-list"><div><span>Participation</span><b>{rows.filter(r=>r.quizAttempts).length}/{rows.length||0}</b><small>learners with a recorded quiz</small></div><div><span>High scorers</span><b>{topQuiz.filter(x=>x.best>=80).length}</b><small>learners reaching 80%+</small></div><div><span>Needs practice</span><b>{topQuiz.filter(x=>x.avg<60).length}</b><small>average below 60%</small></div><div><span>Quiz tests available</span><b>{quizStats.tests.length}</b><small>{quizStats.loading?'Loading…':'team assessments'}</small></div></div></section></div></section>}
  {tab==='achievements'&&<section className="leader-analytics"><div className="analytics-hero achievement-hero"><div><p className="eyebrow">GAMIFICATION</p><h2>Milestones & badges</h2><p>Small wins keep the team moving. Badges are earned from learning activity already recorded in LearningBeyond.</p></div><div className="badge-orbit">✨</div></div><div className="achievement-grid">{achievements.map((a,i)=><article key={a.user_id+'-'+a.title} style={{'--delay':i*45+'ms'}}><span>{a.icon}</span><div><b>{a.title}</b><strong>{a.name}</strong><small>{a.detail}</small></div></article>)}{!achievements.length&&<div className="empty-state"><b>Your first badge is waiting.</b><p>Complete lessons, score highly on quizzes or build a learning streak.</p></div>}</div><div className="leader-stats-strip"><div><span>⚡ Total team XP</span><strong>{totalXp}</strong></div><div><span>🔥 Best streak</span><strong>{Math.max(0,...rows.map(r=>r.streak))} days</strong></div><div><span>🎯 Best quiz</span><strong>{quizBest||0}%</strong></div><div><span>🏆 Badges earned</span><strong>{achievements.length}</strong></div></div></section>}
  <section className="invite-strip"><div><span className="invite-icon"><Icon type="users"/></span><div><h3>Learning is more fun together!</h3><p>Invite your team, take courses and turn progress into momentum.</p></div></div><button onClick={invite}>Invite teammates <span>→</span></button></section>
  {selected&&<div className="leader-profile-backdrop" onClick={()=>setSelected(null)}><div className="leader-profile-modal" onClick={e=>e.stopPropagation()}><button className="leader-modal-close" onClick={()=>setSelected(null)}>×</button><div className="leader-profile-head"><span className="member-avatar big">{initials(selected.m.profiles?.display_name)}</span><div><p className="eyebrow">LEARNER PROFILE</p><h2>{selected.m.profiles?.display_name||'Member'}</h2><p>{selected.m.profiles?.learning_goal||'Learning journey'}</p></div></div><div className="leader-profile-metrics"><div><b>{selected.xp}</b><span>Learning XP</span></div><div><b>{selected.percent}%</b><span>Course completion</span></div><div><b>{selected.quizAvg||0}%</b><span>Quiz accuracy</span></div><div><b>{selected.streak}</b><span>Day streak</span></div></div><div className="leader-profile-note">XP is a gamification metric calculated from recorded learning activity; it is not a grade or stored score.</div></div></div>}
 </div>
}