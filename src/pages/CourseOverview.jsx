import{useTeam}from'../context/TeamContext';export default function CourseOverview({courseId,openPlayer,back}){const{courses,videos,progress}=useTeam(),p=courses.find(x=>x.id===courseId),vs=videos[courseId]||[];const done=vs.filter(v=>progress[v.id]?.completed_at).length,pc=Math.round(done/(vs.length||1)*100);if(!p)return null;return <div className="page"><button className="back" onClick={back}>← Back to courses</button><section className="hero course-hero"><p className="eyebrow">COURSE OVERVIEW</p><h1>{p.title}</h1><p className="muted">{p.channel||'Course'} · {vs.length} lessons</p><div className="progress"><i style={{width:pc+'%'}}/></div><p className="muted">{done} of {vs.length} completed · {pc}%</p><button onClick={()=>openPlayer((vs.find(v=>!progress[v.id]?.completed_at)||vs[0])?.id)}>▶ Continue learning</button></section><section className="section"><div className="section-head"><h2>Course content</h2><span>{vs.length} lessons</span></div>{vs.map((v,i)=>{const pos=progress[v.id]?.last_position_seconds||0;const resume=pos?'Resume at '+Math.floor(pos/60)+':'+String(Math.floor(pos%60)).padStart(2,'0'):'Not started';return <div className="overview-video" key={v.id}><div className="grow"><b>{i+1}. {v.title}</b><p>{progress[v.id]?.completed_at?'✓ Completed':resume}</p></div><button className="ghost" onClick={()=>openPlayer(v.id)}>Open</button></div>})}</section></div>}