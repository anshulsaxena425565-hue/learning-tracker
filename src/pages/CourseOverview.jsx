import{useTeam}from'../context/TeamContext';export default function CourseOverview({courseId,openPlayer,back}){const{courses,videos,progress}=useTeam(),p=courses.find(x=>x.id===courseId),vs=videos[courseId]||[];const done=vs.filter(v=>progress[v.id]?.completed_at).length,pc=Math.round(done/(vs.length||1)*100),next=vs.find(v=>!progress[v.id]?.completed_at)||vs[0];if(!p)return null;return <div className="page"><button className="back" onClick={back}>← Back to courses</button><section className="course-hero"><div className="course-hero-copy"><p className="eyebrow">COURSE OVERVIEW</p><h1>{p.title}</h1><p className="muted">{p.channel||'Course'} · {vs.length} lessons</p><div className="course-progress-line"><div><b>{pc}% complete</b><span>{done} of {vs.length} lessons</span></div><div className="progress"><i style={{width:pc+'%'}}/></div></div><button onClick={()=>openPlayer(next?.id)}>▶ {done?'Continue learning':'Start learning'}</button></div><div className="hero-ring large" style={{'--pct':pc+'%'}}><b>{pc}%</b></div></section><section className="section"><div className="section-head"><div><p className="eyebrow">CURRICULUM</p><h2>Course content</h2></div><span className="pill">{done}/{vs.length} complete</span></div><div className="lesson-list">{vs.map((v,i)=>{const pos=progress[v.id]?.last_position_seconds||0,completed=!!progress[v.id]?.completed_at;return <button className={'lesson-row '+(completed?'done':'')} key={v.id} onClick={()=>openPlayer(v.id)}><span className="lesson-number">{completed?'✓':String(i+1).padStart(2,'0')}</span><span className="grow"><b>{v.title}</b><small>{completed?'Completed':pos?'Resume at '+Math.floor(pos/60)+':'+String(Math.floor(pos%60)).padStart(2,'0'):'Not started'}</small></span><span>→</span></button>})}</div></section></div>}