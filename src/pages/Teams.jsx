import{useTeam}from'../context/TeamContext';export default function Teams(){const{teams,team,switchTeam}=useTeam();return <div className="page"><header><div><p className="eyebrow">WORKSPACES</p><h1>Your teams</h1></div></header><div className="course-grid">{teams.map(t=><section className="section" key={t.id}><h2>{t.name}</h2><p className="muted">Code: {t.code} · Role: {t.role}</p><button disabled={team?.id===t.id} onClick={()=>switchTeam(t.id)}>{team?.id===t.id?'Current team':'Switch team'}</button></section>)}</div></div>}